services:
  app:
    build: .
    image: wall-app:latest
    container_name: wall-app
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/data
    ports:
      - "127.0.0.1:8001:8000"
